const AppHeader = () => {
  return (
    <div className="sticky top-0 z-50 flex h-12 items-center justify-between px-4 border-b -backdrop-blur-xs">
      <p>Button</p>
      <p>venato</p>
      <p>theme switcher</p>
    </div>
  );
};

export default AppHeader;
